system: |
  Extract all {ioc_type} from the text.

  Definition: {ioc_definition}

  Examples:
  {examples}

  Rules:
  - Extract ONLY {ioc_type}
  - Keep defanging as-is (hxxp, [.], etc.)
  - Include full URLs with paths
  - Look in IOC/C&C sections
  - Return valid JSON array only

  Text:
  {context}

  Return JSON array of {ioc_type} found, or [] if none.
